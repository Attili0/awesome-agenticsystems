[pytest]
testpaths = tests
markers =
    slow: runs a real `npm run build` per case. Skip with -m "not slow".
# -r fE surfaces the reason a test was skipped, which matters for the slow
# marker: a silently skipped suite looks the same as a passing one.
addopts = -q -r fE
